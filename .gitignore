# Swift / SPM
.build/
build/
*.xcodeproj
.swiftpm/

# Docs
site-build/

# Python / uv
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Rust core (Phase 1) — build output + generated FFI artifacts
core/target/
core/artifacts/

# macOS
.DS_Store

# Local secrets (Sparkle private key, etc.) — never commit
.secrets/

# Files below are git-ignored AND synced across machines via envsecrets
# (age-encrypted to GCS). Do not remove or modify the "# envsecrets" marker line;
# everything between it and the next blank line is the tracked-file list.
# envsecrets
.secrets/cert.p12
.secrets/apple.env
